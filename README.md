# Theatre-Seat-Booking-Application
 built a theatre seat booking system where users can view shows and book seats in real time. To prevent double booking, I designed the database schema carefully and handled seat locking at the backend. I used MySQL transactions to ensure consistency, so once a seat is booked, it cannot be booked again concurrently.  
